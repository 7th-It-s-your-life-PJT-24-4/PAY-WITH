package com.paywith.notification.domain;

/** notifications.type ENUM 과 이름이 일치한다. */
public enum NotificationType {

    APPROVAL_REQUEST,
    APPROVAL_RESULT,
    ANOMALY,
    PAIRING,
    TRANSACTION
}
