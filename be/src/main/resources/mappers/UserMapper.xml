<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.paywith.mapper.UserMapper">
  <resultMap id="UserResultMap" type="com.paywith.domain.User">
    <id property="id" column="id" />
    <result property="email" column="email" />
    <result property="password" column="password" />
    <result property="name" column="name" />
    <result property="createdAt" column="created_at" />
    <result property="updatedAt" column="updated_at" />
  </resultMap>

  <select id="findAll" resultMap="UserResultMap">
    SELECT id, email, password, name, created_at, updated_at
    FROM users
    ORDER BY id DESC
  </select>

  <select id="findById" resultMap="UserResultMap">
    SELECT id, email, password, name, created_at, updated_at
    FROM users
    WHERE id = #{id}
  </select>

  <select id="findByEmail" resultMap="UserResultMap">
    SELECT id, email, password, name, created_at, updated_at
    FROM users
    WHERE email = #{email}
  </select>

  <insert id="insert" parameterType="com.paywith.domain.User" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO users (email, password, name)
    VALUES (#{email}, #{password}, #{name})
  </insert>

  <update id="update" parameterType="com.paywith.domain.User">
    UPDATE users
    SET name = #{name}
    WHERE id = #{id}
  </update>

  <delete id="delete">
    DELETE FROM users
    WHERE id = #{id}
  </delete>
</mapper>
