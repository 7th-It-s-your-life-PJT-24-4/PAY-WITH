<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.paywith.mapper.UserMapper">
  <resultMap id="UserResultMap" type="com.paywith.domain.User">
    <id property="id" column="user_id" />
    <result property="role" column="role" />
    <result property="phone" column="phone" />
    <result property="password" column="password" />
    <result property="name" column="name" />
    <result property="birthDate" column="birth_date" />
    <result property="gender" column="gender" />
    <result property="fcmToken" column="fcm_token" />
    <result property="status" column="status" />
    <result property="createdAt" column="created_at" />
    <result property="updatedAt" column="updated_at" />
  </resultMap>

  <select id="findAll" resultMap="UserResultMap">
    SELECT user_id, role, phone, password, name, birth_date, gender, fcm_token, status, created_at, updated_at
    FROM users
    ORDER BY user_id DESC
  </select>

  <select id="findById" resultMap="UserResultMap">
    SELECT user_id, role, phone, password, name, birth_date, gender, fcm_token, status, created_at, updated_at
    FROM users
    WHERE user_id = #{id}
  </select>

  <select id="findByPhone" resultMap="UserResultMap">
    SELECT user_id, role, phone, password, name, birth_date, gender, fcm_token, status, created_at, updated_at
    FROM users
    WHERE phone = #{phone}
  </select>

  <insert id="insert" parameterType="com.paywith.domain.User" useGeneratedKeys="true" keyProperty="id" keyColumn="user_id">
    INSERT INTO users (role, phone, password, name, birth_date, gender)
    VALUES (#{role}, #{phone}, #{password}, #{name}, #{birthDate}, #{gender})
  </insert>

  <update id="update" parameterType="com.paywith.domain.User">
    UPDATE users
    SET name = #{name}
    WHERE user_id = #{id}
  </update>

  <delete id="delete">
    DELETE FROM users
    WHERE user_id = #{id}
  </delete>
</mapper>
